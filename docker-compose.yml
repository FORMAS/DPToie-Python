services:
  ptoie_dep:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ptoie_dep
    volumes:
      - .:/ptoie_dep
    command: poetry run python3 src/main.py -path /ptoie_dep/inputs/teste.conll -conll -cc -sc -hs -a -t
    environment:
      - STANZA_RESOURCES_DIR=/ptoie_dep/models/.stanza_resources